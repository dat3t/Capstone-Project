

@using Microsoft.AspNet.Identity
@model List<OneVietnam.Models.PostViewModel>
 
     @foreach (var post in Model)
     {
         @Html.Partial("../Shared/_ShowPostDetail",post)

         <script>
             $('grids').isotope('layout')
         </script>
     }